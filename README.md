#install my project game-hub from git --- coomand----> git clone https://github.com/michail2003/game-hub.git

# packets needed :
  # pip install django
  # pip install pillow

# run the app --> python manage.py runserver
